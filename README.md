# Note: Working on the web app currently
- CI pipeline is running on filter: web only
*CLEAR FILTER WHEN WORKING ON ADMIN NEXT
- Playwright config file removed 3002 for admin
*UNCOMMENT TO RUN admin tests
*Re-add dependencies
*useBaseURL = back to 3002